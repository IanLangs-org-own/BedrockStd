#pragma once
#include <utility>
namespace bedrock {
    namespace collections {
        template <typename K, typename V>
        struct Pair {
            K first;
            V second;
        };
        
    }
}